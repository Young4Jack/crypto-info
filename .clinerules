# 核心角色与技术栈
你负责开发 "Crypto-info" 系统。技术栈：Python, FastAPI, SQLAlchemy (后端)；Vue.js (前端)；SQLite (数据库)。

# 【最高优先级】终端防卡死与进程管理协议
你的运行环境极其脆弱，执行命令时如果触发阻塞或交互提示，会导致系统永久瘫痪。在调用终端工具前，必须通过以下安全检查：

1. 禁止运行守护进程与服务器：
   - 绝对禁止执行 `npm run dev`, `npm run serve`, `python run.py`, `uvicorn`, `celery` 等任何持续运行的命令。
   - 默认前端 (Vue) 和后端 (FastAPI) 服务已由人类在独立终端运行。如果你需要测试，假设服务在本地端口已就绪。

2. 强制非交互模式 (Non-interactive)：
   - 执行安装或初始化时，必须消除所有等待人类输入的环节。
   - npm/yarn 必须加 `-y` 或 `--yes` (如 `npm init -y`)。
   - 文件删除必须用 `rm -f`，禁止使用 `rm -i`。

3. 禁用分页器 (No Pagers)：
   - 绝对禁止使用 `less`, `more`, `vi`, `vim`, `nano`。
   - 执行 git 命令时，如果可能产生长输出，必须前置 `--no-pager` 参数 (如 `git --no-pager log -n 5`)。

4. 限制输出流体积 (Buffer Control)：
   - 读取日志或大文件时，禁止使用 `cat` 打印全量内容。必须使用 `head -n 50`, `tail -n 50` 或 `grep`。
   - 禁止对大型目录（如 node_modules, .git, venv）执行无深度的 `tree` 或 `ls -R`，必须限制层级（如 `tree -L 2`）。
   - 网络请求类命令（如 curl）必须设置超时时间，防止网络挂起（如 `curl -m 10`）。

5. 严禁复杂内联脚本与交互式 REPL (防解析挂起)：
   - 绝对禁止使用 `python3 -c "..."` 执行包含换行符或复杂引号嵌套的多行代码，极易导致终端转义解析失败并挂起。
   - 绝对禁止输入纯 `python3` 或 `sqlite3` 等命令进入交互式 Shell 环境。指令必须包含完整的执行与退出动作。

# 【核心架构与数据库操作规范】

1. 数据库结构确认协议 (SQLite 特化)：
   获取或核对数据库表结构时，必须严格按以下优先级执行，禁止跳级：
   - [首选] 静态代码分析：直接读取并分析项目中的 SQLAlchemy 模型文件（如 `backend/app/models.py`）。
   - [备选] 原生 CLI 单行命令：使用单行命令查询物理数据库，如 `sqlite3 backend/crypto.db ".schema users"`。
   - [末位] 临时脚本执行：若查库逻辑极其复杂，必须将代码写入临时文件（如 `temp_check.py`），执行 `python3 temp_check.py` 提取结果后，必须立即执行 `rm temp_check.py` 清理现场。

2. 架构与依赖红线：
   - 当前项目技术栈底线为：FastAPI + SQLAlchemy + SQLite。
   - 必须在现有环境下解决业务逻辑、并发或性能问题（例如：调整 SQLite 连接池参数、优化 SQLAlchemy 查询）。
   - 绝对禁止为了规避问题而私自引入 Redis、Celery、RabbitMQ 等外部依赖，或擅自提议更换为 PostgreSQL/MySQL。


# 工程规范
1. 修改现有文件前，必须先读取分析现有逻辑。
2. 未经明确授权，禁止修改 FastAPI 数据模型、SQLite 表结构及 Vue 路由核心架构。

# 忽略依赖和环境
node_modules/
venv/
.venv/
__pycache__/

# 忽略构建产物
dist/
build/

# 忽略数据库和备份
*.db
*.db.bak
*.sqlite

# 忽略不必要的工具配置
.vscode/

# 日志处理规范
1. 禁止直接使用 `read_file` 读取超过 1MB 的日志文件。
2. 必须优先使用 `grep`, `tail` 或 `awk` 在终端过滤日志关键字。
3. 如果日志文件较大，只允许读取最后 100 行进行分析。